# Design QA：macOS 原生设置界面与 Liquid Glass

[English](design-qa.en.md)

## 审查范围

- 设置窗口：默认 `920 × 700`、最小 `800 × 650`，可自由缩放；
- 首次使用 Onboarding：约 1020×772，不显示步骤编号、总数、百分比、“跳过”或“稍后继续”；
- 页面：连接与语音、按键映射、统计、回眸、权限与隐私、关于；
- 各页面主标题由原生统一工具栏显示，内容区不再重复页面标题或解释型副标题；
- 仓库截图：
  - [连接与语音](Screenshots/connection-and-voice.png)
  - [按键映射](Screenshots/key-mapping.png)
  - [权限与隐私](Screenshots/permissions-and-privacy.png)

## 当前实现

- 未完成当前流程版本时，主窗口使用 Typeless 式双栏 Onboarding：顶部仅显示“准备 › 设置 › 试一下”，实体遥控器路径九页、无遥控器路径十页，均在 `1020 × 772` 内一次完整展示、不使用内部滚动；先并列询问“有遥控器 / 当前没有遥控器”，仅无遥控器时下一页选择推荐 iPhone App 或“没有 iPhone 时”使用网页版；左侧完成当前任务，右侧显示对应控制方式、实时检查或输入法四项设置；音频页只平铺并接受 MiRemoteV 2ch 与 BlackHole 2ch，不使用下拉框；必要能力未通过时主按钮禁用，完成前根视图不显示设置页；
- Onboarding 当前卡点在页面内只突出一个主要修复动作，诊断复制为同卡片中的次要操作；完成页环境变化时直接返回受影响阶段，不新增 Sheet、Popover 或连续确认弹窗；
- 输入法设置右栏的系统 Fn 第 3 项使用匹配浅色/深色的 macOS 键盘设置原图，并继续显示实时冲突状态；截图和状态卡必须在默认窗口内同时可见；
- 设置页根布局使用原生 `NavigationSplitView`，侧栏使用 `.sidebar` 样式的系统 `List` 和系统选择态；侧栏理想宽度为 232pt，搜索与侧栏行在侧栏作用域内采用系统 `.large` 控件密度，内容区和 `NavigationSplitView` 根保持 `.regular`；`.toolbar(removing: .sidebarToggle)` 必须直接作用于 sidebar 列，移除不属于系统设置信息架构的侧栏开关；使用 `.searchable(placement: .sidebar)` 提供固定在工具栏下方的系统搜索框，搜索具体设置项时右侧页面保持可见，结果同时显示所属页面并可定位到对应分区；`800 × 650` 下全部入口仍保持可见；
- “连接与语音”使用原生分组 `Form`、`Section`、`LabeledContent`、单选 `Picker`、音频设备 `Picker`、`Slider` 和标准按钮组织遥控器、语音输出、兼容麦克风及移动端入口，不再使用自定义玻璃卡片或状态胶囊模拟系统设置；语音输出只由 Picker 行和操作按钮启用态承担健康状态，不得显示“可发送测试音”等被动 ready 复读，只有未选择、不可用或失败时显示独立状态行；播放中、完成、取消和错误等测试音反馈必须与三个音频操作位于同一个 Form 行，不在两者之间生成分隔线，也不得重复音频状态；音频路由说明属于整个原生 Section footer，不放入自绘卡片；页面可以滚动但不显示滚动条；
- 侧栏选择完全沿用系统源列表高亮；按键映射中的实体按键选择态继续使用低透明度语义蓝，避免把业务对象选择误装成新的系统导航组件；
- 正文、辅助说明、分组标题和状态分别使用 SwiftUI 的 `.body`、`.callout`、`.headline` 等系统语义字体；不再用硬编码 12/13/14pt 模拟系统层级。中文最终字号不得小于 12pt；
- 侧栏图标使用 20pt 系统色分类底板与 12pt 单色 SF Symbol、5pt 连续圆角和轻微系统式投影，不添加描边；颜色取自 AppKit 动态系统色并保留原生源列表选择背景。侧栏行首为 9pt，图标与文字间距为 7pt，文字起点与系统设置一致；不自绘选中态，也不把普通说明文字染成强调蓝。分类色只帮助区分入口，连接、警告和成功继续使用对应语义状态色；
- 窗口保留原生红黄绿按钮、可见逻辑标题和系统统一标题栏；背景、分隔与滚动后的遮蔽全部交给 AppKit 的 `.unified` toolbar 默认行为，不得强制 `titlebarAppearsTransparent` 或关闭标题栏分隔；标题栏使用原生大号 `ControlGroup(.navigation)` 提供返回、前进及前进历史菜单，标题栏也是唯一窗口拖动区域，不再在 SwiftUI 内容中加入自绘拖动条或圆形导航按钮；
- macOS 26/27 的导航组直接使用系统 `ControlGroup(.navigation)`、`.buttonStyle(.glass)` 与 `.controlSize(.extraLarge)`；不调用 `glassEffect` 绘制外壳，不自定圆角、背景或控件尺寸。构建必须保持最低 macOS 14 的同时链接当前 Xcode SDK，使标题栏、工具栏、侧栏和标准控件由系统提供 Liquid Glass 外观；macOS 14/15 使用同一原生导航结构的对应系统样式；
- 所有页面只允许三类对齐：普通设置使用左标签/右控件，整块业务内容使用分区左边线，分段筛选等完整控件占满分区可用宽度。普通 `GroupBox` 不得依赖 `LabeledContent` 自动生成局部标签列，避免在页面中部形成第三条边线；
- 只解释单个设置项的辅助文字必须与该设置控件共同组成一个 `Form` 行，不能作为紧随其后的独立行而产生中间分隔线；只有解释整个分区的文字才使用 `Section` footer；
- 按键页面复用 `Resources/RC003-remote-photo.png`，保持 508×1030 原始比例；页面容器必须使用与连接页一致的原生 grouped `Form / Section`，不得叠加手工页边距、外层 `GroupBox` 或自定义 `contentMargins`，顶部安全区完全交给 Form 管理；标题栏背景与分隔由系统 `.unified` toolbar 统一管理，不得让设备卡或关系图文字直接穿入标题栏，也不得为单个页面绘制局部材质或添加 scroll-edge 兼容修饰器；顶部启用开关使用明确的左标签和右控件，“遥控器”作为块标题、设备卡独占下一行并铺满分区可用宽度，不得受普通设置控件列或固定最大宽度限制；关系图透明行沿分区左边线展开，不再叠加 Form 行内缩；底部两个设置行继续使用左标签和右控件，HID 状态整行左对齐，恢复默认整行右对齐；
- 按键关系图的左右模块保持 64pt 高，相邻模块净间距不得小于 16pt；语音键与全部实体按键模块必须完整落在画布内，连接线继续落在各模块垂直中心；
- 普通实体按键按下时会高亮遥控器示意图并定位映射行，语音键使用独立的语音活动状态；
- 界面未展示实物上不存在的独立静音键。
- 普通界面使用产品语言，不显示遥控器型号代号、蓝牙语音协议名、按键协议名、十六进制按键编号或设备标识术语；
- 遥控器设备卡只用标准电池图标和百分比表达电量；空电、低电、正常和满电使用不同电池符号与语义颜色，充电或外接电源时在图标上叠加闪电，不再额外显示“可充电电池”文字；
- 统计页使用 grouped `Form`：周期选择直接使用居中的原生 `Picker(.segmented)`，macOS 26/27 使用系统 `extraLarge` 密度，macOS 14/15 使用系统 `large` 回退；480pt 仅作为居中布局槽，不固定控件高度或自绘拉伸。继续使用真实存在的“日 / 周 / 全部”三个周期，不重复显示页面标题作为左标签，不通过 `NSViewRepresentable` 改写系统选中态；该行背景必须透明，只保留分段控件自身的系统外观；“数据仅存本机”作为全页唯一的本机存储提示；两张图共享一个数据分区，排行位于后续分区且只解释排行口径。只有柱状图和排行内容保留业务数据可视化，不再给每张图重复增加 `GroupBox` 边界；分享不在统计页重复出现；
- “回眸”作为紧跟“统计”的独立侧边栏页面，使用 grouped `Form` 把记录开关、空状态/历史、本地 Agent 访问拆成连续系统 `Section`；记录用途与隐私共同归入开关分区 footer，空状态只表达当前启用状态，Agent 隐私属于 Agent 分区 footer，不在卡片内外悬空。关闭/空状态使用左图标和左对齐说明，不占用大块居中空白；“本地 Agent 访问”是 section header，“允许访问”是功能行标签。页面默认展示全部 App 的倒序时间线，顶部应用栏固定显示“全部应用”，其余 App 按最近使用排序并支持单行横向浏览或在页面内展开为自适应网格，图标无法解析时回退通用 App 图标；日期分组默认展开最新一天，并支持逐日展开或折叠；本地 Agent 访问使用页面内开关、文本框、复制按钮和授权列表完成配置，不使用下拉框、Popover 或 Sheet；关闭时仍可查看和撤销授权；
- 网页连接邀请码弹窗顶部重点推荐 iOS App，TestFlight 公测地址不直接显示，并提供打开页面和复制链接操作；
- 权限页的蓝牙、输入监控和辅助功能只占三个功能行；升级后权限延续说明必须位于“所需权限”分区 footer，不得伪装成第四个权限项目；
- 关于页使用 grouped `Form` 与系统 `Section`，把支持、版本、配置和应用偏好按同一标签列/操作列排列；分享二维码只在“关于 → 更新与支持”保留一个入口，侧边栏与统计页不得再放置同一命令；配置分区左侧只显示操作说明，右侧保留“导出配置…”或“导入配置…”按钮，不能让同一动作名在一行左右重复；“当前版本”的标题和紧凑检查状态位于 `LabeledContent` 左侧标签列，版本号与唯一更新动作位于尾部控件列，禁止把状态堆在按钮下方形成悬空右侧小模块；“已是最新版本”只显示一次结论，不追加同义说明。语言使用原生分段 `Picker` 的固有尺寸贴齐同一尾部线，不增加固定宽度 frame；“当前版本”只保留一个状态化更新按钮：空闲时检查、已检查或失败时重新检查、发现更新时更新到目标版本、检查中禁用，不能同时出现“检查更新”和“重新检查”；“新版本内容”行只在发现新版本时出现并只展示该版本的更新日志，不能混入检查状态；版本历史是关于页的子页面，使用系统前进指示的导航行进入统一导航历史，标题栏返回/前进可回退，不再使用 Sheet；App 图标为 48pt，版本号回到 `.body` 层级，不使用 28pt 数据卡或 34pt 装饰图标；语言选项始终完整展示，不使用下拉框；官网和 GitHub 保持可见，术语表与退出按钮不再显示；
- 设置窗口使用标准 macOS 菜单栏：App 菜单（关于、隐藏 ⌘H、隐藏其他 ⌥⌘H、全部显示、退出 ⌘Q）、文件、编辑（含“查找 ⌘F”聚焦搜索框，`.searchFocused` 为 macOS 15+ 能力，macOS 14 上菜单项禁用）、显示（返回 ⌘[、前进 ⌘]）与窗口菜单（最小化 ⌘M、缩放、全部移到前台，注册为 `NSApp.windowsMenu`）；窗口位置由系统 frame autosave 记忆，仅无存档时居中；侧栏固定 232pt 不可拖宽；后退与前进按钮对称提供历史菜单；状态栏“关于”直接进入设置关于页，不再使用 NSAlert；菜单命令经 `SettingsNavigationCoordinator` 路由进 SwiftUI，返回/前进/查找按窗口与历史状态启停；
- 侧栏搜索支持完整键盘闭环：回车激活首个结果，结果列表支持方向键选择与 Return 激活；命中分区在滚动定位后显示一次短暂 accent 描边淡出；搜索目录必须随新设置行同步补全，遗漏可操作设置视为缺陷，也不得为已删除的重复入口保留搜索结果；
- 等待中的连接状态（iPhone、Apple Watch、网页版）在状态文字旁显示 `.small` 系统 `ProgressView`，不只依赖颜色文字；iOS App 的“打开 TestFlight / 复制链接”是该设置的辅助操作，必须与标题、说明、状态和“连接手机”保留在同一个 Form 行，辅助操作在行内尾部对齐，Apple Watch 前才出现下一条系统分隔线；
- 卡片式选择按钮（设备卡、动作网格、聚焦策略、自定义 App 选择）统一使用 `SelectableCardButton`：低透明度语义蓝选中、悬停加深与系统式键盘焦点环，选中状态暴露 `.isSelected`；不再各处手写选中底与描边；
- 映射页内容列与其他页同为 700pt，关系图画布按可用宽度自适应；编辑面板出现/消失使用系统过渡动画；
- 所有界面文案使用稳定语义 key；当前语言缺少 Markdown 帮助时回退英文。

## 交互与字体硬性规范

- 中文界面文字的最终显示字号不得小于 12pt。不得使用 8～11pt 的中文文字，也不得通过 `minimumScaleFactor` 把中文缩小到 12pt 以下；空间不足时优先增加控件高度、调整布局、允许换行或对次要内容做尾部截断。
- 尽量不使用下拉列表，尤其避免把基础按键、系统动作、自定义动作和具体 APP 混在同一个长列表中。选项较多时必须按语义分区，优先使用页面内按钮网格、分段选择或清晰的分组列表。
- 能在页面内铺平完成的流程尽量不使用 Popover、Sheet 或连续确认弹窗。相关配置应集中到同一个大页面中，让用户看到当前对象、可选动作、附加设置、学习状态和测试入口，并一次性完成操作。
- 仅在系统文件选择、必要权限授权和不可恢复的危险操作中保留系统弹窗；普通说明、学习进度、成功和失败反馈应优先在当前页面内显示。

## 代码对应位置

- 窗口创建与最小尺寸：`Sources/RemoteMic/RemoteMicApp.swift`；
- Onboarding 根视图、布局和门禁：`Sources/RemoteMic/RemoteMicRootView.swift`、`Sources/RemoteMic/OnboardingView.swift`、`Sources/RemoteMic/OnboardingFlow.swift`；
- 页面布局、材质和遥控器热点：`Sources/RemoteMic/SettingsView.swift`；“回眸”内容布局与 App 图标：`Sources/RemoteMic/TranscriptHistorySection.swift`；
- 实体按键活动状态：`Sources/RemoteMic/HIDRemoteMonitor.swift` 与 `Sources/RemoteMic/BridgeAppModel.swift`。

## 结论

当前仓库截图对应 macOS 26 的系统设置结构；同一页面结构在 macOS 14/15 自动切换到对应系统样式。仓库未保留依赖本机临时目录的审查引用。

本轮原生设置重构的当前结构与约束已合并到“当前实现”，不再按截图轮次重复记录中间方案。自动化、成品构建、真实窗口复核和仍需现场验收的边界，以 `Testing/NativeSettingsInterface.md` 的“当前候选分支验证摘要”为唯一事实源；每个已修复问题的复现、日志、根因和验证证据保留在对应 `Bugs/` 文档中。

首次 Onboarding 已通过编译、自动化和静态字号检查。锁屏状态下已直接实例化生产 `OnboardingView`，通过离屏 AppKit 窗口缓存并逐页检查实体遥控器浅色/深色各 9 张、iPhone 和网页版浅色/深色各 10 张，共 58 张真实实现截图；音频页只展示 MiRemoteV 2ch 与 BlackHole 2ch。`1020 × 772` 内容区、原生窗口框架、底部导航和右侧实时检查均无裁切。右侧插画与检查区跟随系统外观：浅色使用浅蓝画布，深色使用深蓝画布，左右区域不会形成黑白分栏。真实环境验收仍按 `Testing/FirstRunOnboarding.md` 执行，不能用截图替代 RC003、iPhone Nearby、手机 Safari 网页版、系统权限、音频回环设备和第三方语音工具验证。

2026-08-11 全流程恢复审计补充了遥控器 HID 状态与“重新检测按键”、音频页回到前台自动刷新，以及完成页对当前权限、BLE 和音频的最终重验。当时的浅色、深色各 8 张生产 Onboarding 截图已逐张检查；设备卡新电池图标仍需在浅色、深色生产设置页和真实充电线状态下复验。

## 2026-08-16 组合动作页面 QA

- 参考：用户提供的新建前后、组合动作编辑及遥控器触发方式截图；实现：`1.8.24 (117)` 本地 ad-hoc 构建，窗口内容约 `1024 × 768`。
- 已在同一视觉对比中检查参考与实际页面：页面信息层级保持一致，遥控器固定尺寸，新建前后不会缩放；名称标签和输入框同行；步骤编号与动作标题字号、字重一致；App 图标、名称和 Bundle ID 位于同一信息行。
- “测试动作”与可用状态的“保存修改”使用主要按钮样式；失焦或禁用时按照 macOS 系统规则降低强调，不额外伪造蓝色。
- 遥控器触发方式文字会根据按键位置做纵向动画：左键位于右上区域，TV 键位于右下区域；点击页面热点和真实 HID 按键都更新当前按键。
- 已安装 App 网格保持收起式展示；当前机器实际识别无线麦、Codex、Claude、cmux、飞书、微信、天气、豆包、OpenCode、剪映、Cursor、Xcode、网易云音乐、Chrome、Safari 和 Zed。WorkBuddy 按 `com.workbuddy.workbuddy` 过滤，需在安装该 App 的用户机器复验。
- 实测名称超过 20 字符会截断，回车保存后焦点退出输入框；列表删除辅助操作可删除临时组合动作并显示成功状态。
- final result: passed。真实遥控器触发提示和 WorkBuddy 显示仍列入用户机器人工验收，不以当前机器截图替代。

2026-08-17 语音工具页移除左栏内部滚动，四个工具卡统一高度并顶部对齐；豆包/微信四项输入法设置迁移到右栏。新增两层控制方式选择后，实体遥控器浅色/深色各 9 页、iPhone 和网页版浅色/深色各 10 页，以及豆包四种右栏指引状态和微信浅色/深色语音工具页均已用生产视图复验，在 `1020 × 772` 下无内部滚动、错位或裁切。

2026-08-17 控制选择改为两层：第一层并列显示“有遥控器 / 当前没有遥控器”并优先推荐实体遥控器；只有选择没有遥控器，第二层才显示推荐 iPhone App 与“没有 iPhone 时使用网页版”。生产状态已经确认遥控器连接时自动跳过第一层，但不会为判断提前启动 discovery。三种控制方式统一显示蓝牙、输入监控和辅助功能权限，连接页分别显示实体遥控器、Nearby 或二维码/WSS 状态。

2026-08-17 系统 Fn 第 3 项加入 macOS 键盘设置浅色/深色原图。生产视图已检查浅色、深色正常状态及浅色冲突状态，截图、实时说明、紧凑“打开键盘设置”按钮和底部导航均同时可见，未重新引入内部滚动。

## 2026-08-18 回眸页面验证

- 确认稿：`dist/designs/reflections-layout/expanded-app-switcher-collapsible-timeline.png`；实际实现截图：`dist/designs/reflections-layout/implemented-expanded.png`；并排比较图：`dist/designs/reflections-layout/reference-vs-implemented.png`。
- 在生产设置窗口中确认“回眸”紧跟“统计”，页头隐私说明与最右侧开关完整可见；顶部应用栏可在单行和页面内网格之间展开、收起，“全部应用”始终可见。
- 使用当前真实的 2 个 App、1 个日期、12 条记录确认单 App 筛选、倒序时间线、每条记录的 App 图标和名称、日期折叠恢复，以及页面底部无包围样式的“全部删除”；验证期间未执行任何删除。
- 生产窗口当前最小尺寸高于 `800 × 650`，本轮按实际生产窗口完成检查，不能表述为已在 `800 × 650` 生产窗口验收。10 App 横向滚动、跨日期默认折叠和浅色/深色仍需对应数据与外观环境复验。

final result: passed（上述真实数据与窗口边界除外）

## 2026-08-18 本地 Agent 访问区验证

- 在当前生产最小窗口中确认本地 Agent 访问区位于完整历史之后、“全部删除”之前；历史仍是页面主内容，新增区域没有制造核心空白或遮挡记录。
- 缺少设置事件时自动化确认默认关闭；实际页面检查时“允许访问”保持关闭，关闭状态仍显示授权列表，没有创建授权、复制令牌或改动历史。
- 中文标题、说明、开关、授权列表和底部操作均完整显示，字号不低于 12pt；页面内没有新增下拉框、Popover 或 Sheet。
- 当前生产窗口最小尺寸高于 `800 × 650`，本轮沿用实际生产最小窗口验证；开启、生成配置、长客户端名称、英文、浅色/深色和真实 MCP 客户端仍按测试手册人工验收。

final result: passed（上述人工状态与窗口边界除外）

## 2026-08-19 组合动作与回眸 MCP 边界说明

- 组合动作页底部增加固定 12pt 中英文信息行，明确“学习输入框”只负责聚焦目标 App，“回眸”中的 Codex MCP / TOML 只负责授权读取本地历史；没有新增副标题、下拉框、Popover、Sheet 或确认弹窗。
- 宿主公开构建和注入最新私有模块的编译、本地化及设置页结构测试通过；组合动作现已无需邀请码直接显示，本次仍未完成中英文、浅色/深色和 `800 × 650` 真实组合动作页面人工验收。

## 2026-08-20 自定义快捷键选择器 QA

- 自定义快捷键编辑区新增页面内“常用组合 / 标准键盘”分段选择，不使用下拉列表、Popover 或 Sheet；原有键盘录入、当前值和清除入口仍位于同一编辑区。
- 常用组合在 `800 × 650` 中文浅色生产 App 视图中完整显示；选择态使用低透明度语义蓝，选择预设本身不会执行快捷键。
- 标准键盘在 `800 × 650` 中文浅色、中文深色和英文浅色生产 App 视图中检查了组合键、主键区和方向导航区；Command 等长标签保持单行，键盘没有横向滚动或横向裁切。
- 同一生产视图以 `800 × 1400` 展开检查了 F1～F20、数字键盘和单独左/右 Command、Option、Control、Shift 与 Fn；全部中文说明和按键文字不小于 12pt。最小高度下通过既有页面纵向滚动访问后续键区，不改变窗口几何。
- 自动化覆盖预设键码、标准键盘关键分区、左右 Option 键码和单独修饰键的按下/释放顺序。离屏生产视图与自动化不能替代实体遥控器、真实 Spotlight、系统权限和左右修饰键目标 APP 的桌面验收，人工步骤见 `Testing/KeyboardShortcutPicker.md`。

final result: passed（真实遥控器与真实系统快捷键验收除外）

### 快速连接补充约束

- Codex、Claude Code、Cursor、OpenCode 使用页面内两列卡片，不使用下拉列表、Popover 或 Sheet；每张卡显示客户端图标或回退图标、名称、检测/连接状态和单一主操作。
- 手动客户端名称与复制配置保留在快速连接卡片之后，作为冲突、JSONC、CLI 失败和其他 MCP 客户端的降级路径。
- 自动连接失败只显示页面内错误文案，不连续弹窗；在 `800 × 650` 检查中必须确认四张卡、手动配置区、授权列表和页面底部“全部删除”可通过现有页面滚动完整到达。
- “已授权客户端”只展示当前有效授权，不展示快速连接失败产生的撤销记录；错误文案必须包含对应客户端名称和可行动的失败原因，不能只显示统一的“自动连接失败”。
- 快速连接成功状态必须显示具体客户端名称，例如“请重新打开 Codex”；说明文案明确无需重启无线麦，避免把“客户端”误解为当前 App。
- App 改名、移动或旧连接缺少 Helper 路径指纹时，客户端卡和授权行使用橙色文字显示“先移除，再重新连接”；提示留在页面内、不弹窗，不把自定义安装路径或令牌展示给用户。
